[package]
name = "unicode_casing_codegen"
publish = false
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
clio = { version = "0.3", features = ["clap-parse"] }
indenter = { version = "0.3", features = ["std"] }